/**
 * NOTE: This file is intentionally left as a no-op. The correct Next.js edge
 * middleware implementation lives in `middleware.ts` (correct spelling).
 *
 * This misspelled file previously contained locale redirect logic, but because
 * Next.js only recognizes the correctly spelled `middleware.ts` filename at the
 * project root, it was never executed at runtime. Keeping this file (empty)
 * with an explanatory comment prevents re‑introducing logic in the wrong place.
 */

export {}; // keep as a module
